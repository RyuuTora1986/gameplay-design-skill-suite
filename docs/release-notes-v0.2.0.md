# Release Notes: v0.2.0

## Release Title

`v0.2.0 - Design-To-Execution Gameplay Pipeline For Codex`

## Summary

This release expands the public gameplay design skill suite into a design-to-execution chain for Codex.

The suite is now built around one stronger operating model:

- `gameplay-design-orchestrator` owns the upstream flow
- downstream `gameplay-*` skills behave as controlled ordered stages
- `game-design-spec` expands the mature package into a full Chinese game spec
- `game-design-execution-compiler` compiles that spec into an agent-executable task plan

## Highlights

- added 9 coordinated game design skills
- locked orchestrator-first topology
- added execution-plan compilation as a new post-spec layer
- included a validated gyro-battle execution-plan example
- included local validators for gameplay package, full spec, and execution plan
- updated launch-ready repo packaging and promo copy for the full chain

## Why This Matters

Many AI game-design repos stop at ideation. This suite now carries the work further:

- direction lock
- repeatable-fun hypothesis
- pacing structure
- scope discipline
- coding-oriented handoff
- spec expansion
- execution-plan compilation

## Suggested GitHub Release Body

Gameplay Design Skill Suite packages an orchestrator-first workflow for Codex and now extends it into execution-plan compilation. Instead of exposing disconnected prompts, it forces direction lock, ordered stage execution, scope control, downstream-ready handoff, full Chinese spec expansion, and agent-executable task planning. The repository includes a validated gyro-battle example, local validators, and launch-ready documentation for commercial packaging.

## Suggested Short Social Copy

Released: a design-to-execution gameplay skill suite for Codex. It turns raw game ideas into validated gameplay packages, expands them into Chinese production-ready specs, then compiles them into agent execution plans. Includes 9 skills, a real gyro-battle example, and local validators.
